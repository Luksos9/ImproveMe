# Copy this file to .env.local and paste your real key.
# The VITE_ prefix is required so Vite exposes it to the browser.
# This app is local-only and personal — the key never leaves your machine.
VITE_ANTHROPIC_API_KEY=sk-ant-...
